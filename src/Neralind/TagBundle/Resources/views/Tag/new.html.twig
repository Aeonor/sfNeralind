{% extends '::layout-admin.html.twig' %}

{% set title %}Ajouter un <strong>tag</strong>{% endset %}

{% block body_admin -%}
<h1>{{ title }}</h1>

<div id="content">
    {{ form(form) }}

    <ul class="record_actions">
      <li>
        <a href="{{ path('tag') }}">
          Back to the list
        </a>
      </li>
    </ul>
  </div>
{% endblock %}
